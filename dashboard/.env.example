# Copiar a .env.local. IP de la PC de escritorio donde corre el servidor
# (Docker: Postgres + Mosquitto + FastAPI). No usar túneles de VS Code acá:
# ver docs/arquitectura.md sobre por qué.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws
